{
  "name": "bb-tournament-validator",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.34.4",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "test": "vitest run",
    "typecheck": "pnpm -r exec tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
