{
  "name": "@bb/fork-ops",
  "version": "0.1.0",
  "description": "Shared FUMBBL40k fork-ops logic (JNLP building, fork coach account provisioning) — used by both the Discord bot and the config-web API so they behave identically.",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap --clean"
  },
  "dependencies": {
    "mysql2": "^3.22.6"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
