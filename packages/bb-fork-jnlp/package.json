{
  "name": "@bb/fork-jnlp",
  "version": "0.1.0",
  "description": "Pure FUMBBL40k fork-join JNLP builder — shared by the Discord bot and the config-web API so both produce byte-identical JNLPs.",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap --clean"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
